# Count cells grouped by curated CellType (neuron / muscle / other).
PREFIX cckg: <https://wormbase.org/resources/connectome/>

SELECT ?cell_type (COUNT(?cell) AS ?count)
WHERE {
  ?cell a cckg:Cell ;
        cckg:cell_type ?cell_type .
}
GROUP BY ?cell_type
ORDER BY DESC(?count)
